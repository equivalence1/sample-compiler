fun a(int x, int y, int z): int
begin
  int n;
  int i;
  int j;

  read(n);
  i := (x + y) * n;
  j := i + z;

  int l;
  l := 0;
  while (l < 5) do
    l := l + 1;
    j := j * l
  od;

  return j
end

fun b(int a): int
begin
  int x;
  int i;

  read(x);
  write(a + x);
  i := 0;

  while (i < 10) do
    i := i + 1
  od;

  return a + x
end

fun c(): int begin
  return 1
end

int var;
var := 123;
if (a(1, 2, 3) < 100) then
  b(1)
elif (a(b(2), 2, 3) < 1000000) then
  b(2)
else
  b(3)
fi
